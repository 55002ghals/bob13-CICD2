<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>올해의 목표</title>
</head>
<body>
    <h1>올해의 목표</h1>

    <% goals.forEach(goal => { %>
        <div>
            <h2><%= goal.title %> (by <%= goal.author %>)</h2>
            <p><%= goal.content %></p>
            <hr>
        </div>
    <% }) %>

    <form action="/add-goal" method="POST">
        <input type="text" name="title" placeholder="제목" required>
        <input type="text" name="author" placeholder="글쓴이" required>
        <textarea name="content" placeholder="내용" required></textarea>
        <button type="submit">등록</button>
    </form>
</body>
</html>